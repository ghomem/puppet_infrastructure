[Unit]
Description=Hello World application in Flask
Wants=basic.target
After=basic.target network.target

[Service]
ExecStart=/usr/bin/gunicorn --bind <%= @ip_address %>:<%= @port %> wsgi:app
KillMode=process
WorkingDirectory=<%= @app_dir %>

[Install]
WantedBy=multi-user.target
